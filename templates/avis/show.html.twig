{% extends 'base.html.twig' %}

{% block title %}Avis{% endblock %}

{% block body %}
    <h1>Avis</h1>

    <table class="table">
        <tbody>
        <tr>
            <th>Rate</th>
            <td>{{ avis.rate }}</td>
        </tr>
        <tr>
            <th>User</th>
            <td>{{ avis.user.nom }} {{ avis.user.prenom }}</td>
        </tr>
        <tr>
            <th>Place</th>
            <td>{{ avis.place.nom }}</td>
        </tr>
        </tbody>
    </table>

    <a href="{{ path('avsavis') }}">back to list</a>

    <a href="{{ path('avsavisEdit', {'id': avis.id}) }}">edit</a>

    <a href="{{ path('avsavisDelete', {'id': avis.id}) }}">delete</a>
{% endblock %}