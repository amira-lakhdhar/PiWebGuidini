{% extends 'base.html.twig' %}

{% block title %}Reclamation Test{% endblock %}

{% block body %}
    <h1>Edit Reclamation</h1>

    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn">{{ button_label|default('Update') }}</button>
    {{ form_end(form) }}

    <a href="{{ path('reclamation') }}">back to list</a>

    <a href="{{ path('reclamationDelete', {'id': reclamation.id}) }}">delete</a>
{% endblock %}