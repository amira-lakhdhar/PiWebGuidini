{% extends 'base.html.twig' %}

{% block title %}FrontTest{% endblock %}

{% block body %}
    <h1>FrontTest</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ front_test.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('front_test_index') }}">back to list</a>

    <a href="{{ path('front_test_edit', {'id': front_test.id}) }}">edit</a>

    {{ include('front_test/_delete_form.html.twig') }}
{% endblock %}
